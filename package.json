{
  "name": "enterprise-kanban",
  "version": "1.0.0",
  "description": "Production-ready Kanban board with Command-Pattern Undo/Redo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "happy-dom": "^12.0.0"
  }
}
